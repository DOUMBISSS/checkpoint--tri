ALGORITHM Tri 
VAR
    i :INTEGER;
    arr:array_integer :[];
BEGIN

        FOR i FROM 0 TO arr.length-1 DO
                    IF (arr[i-1] > arr[i]) then 
                        arr[i] = arr[i-1];
                    END IF 
                     write (arr[i]);
        END_FOR


END




